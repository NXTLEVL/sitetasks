name: Build & publish custom image

on:
  push:
    branches: [dev]
    paths:
      # Skip the build when only docs / unrelated files change.
      - "app/**"
      - "config/**"
      - "lib/**"
      - "modules/**"
      - "frontend/**"
      - "public/**"
      - "Gemfile*"
      - "package*.json"
      - "docker/prod/**"
      - ".github/workflows/build-image.yml"
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Free disk space on runner
        # OpenProject's image build needs ~20 GB. The default runner has ~14 GB
        # free; this strips preinstalled toolchains we don't need.
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android \
                      /opt/hostedtoolcache/CodeQL /usr/local/share/boost
          sudo docker system prune -af
          df -h

      - uses: actions/checkout@v4

      - name: Lowercase image name
        # GHCR requires lowercase. github.repository preserves the original
        # casing, so we downcase it once here and reuse via env.
        run: echo "IMAGE_NAME_LC=${IMAGE_NAME,,}" >> "$GITHUB_ENV"
        env:
          IMAGE_NAME: ${{ env.IMAGE_NAME }}

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/prod/Dockerfile
          target: slim
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LC }}:17-slim
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LC }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LC }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
